<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>register</title>
    <!-- <link rel="shortcut icon" href="/favicon.ico"> -->
    <script src="/plugin/min.js"></script>
    <link rel="stylesheet" href="/css/common.css" />
    <meta name="viewport" content="width=device-width, user-scalable=no" >
  </head>
<body>

<style>

.main {
  display: flex;
  flex-wrap: wrap;
}
@media (min-width: 700px) {
  .signin {
    width: 45%;
    margin: 2%;
  }
  .signup {
    width: 45%;
    margin: 2%;
  }
}
@media (max-width: 700px) {
  .signin {
    width: 90%;
    margin: 5% auto;
  }
  .signup {
    width: 90%;
    margin: 5% auto;
  }
}

input {
  border-radius: 10px;
  width: 70%;
  display: block;
  margin: 10px;
  padding: 10px;
}
/* .error {
  display: none;
} */
input:invalid + .password1::after {
  content: "at least one character, one number, one special character";
  color: red;
}

</style>

<div class="main">
  <div class="signin">
    <div class="login-form-inner">
      <button id="google-login" class="btn-default btn-sns btn-sns-google">
        Googleでログイン
      </button>
      <button id="facebook-login" class="btn-default btn-sns btn-sns-facebook">
        <i class="icon-facebook"></i>
        Facebookでログイン
      </button>
    </div>
    <div>
      <input type="email" placeholder="mail">
      <input type="password" placeholder="password">
      <input type="submit" value="login">
    </div>
  </div>
  <div class="signup">
    <div>
      <form id="register" onsubmit="registering()">
      <input type="email" name="email" placeholder="mail" required 
      pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$" title="must be email format" >
      <input type="password" name="password1" placeholder="password" required
       pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#$%^&*_=+-]).{8,12}$" 
       title="at least one character, one number, one special character" >

      <input type="password" name="password2" placeholder="password confirm" required 
      oninput="check(this)" >
      <input type="text" name="givingName" placeholder="giving name" required pattern="[^/#&?%.\-+_=@ '&quot;]+">
      <input type="text" name="familyName" placeholder="family name" required pattern="[^/#&?%.\-+_=@ '&quot;]+">
      <input type="date" name="birthday" required>
      <input type="submit" value="登録" > 
      <!-- onclick="showErr()" -->
    </form>
      
    </div>
  </div>
</div>


<style>
.footer {
  padding: 10px;
  background-color: black;
}
.footer a {
  padding: 2px 10px;
  font-size: 14px;
  width: 100px;
  display: inline-block;
  vertical-align: middle;
  text-align: left;
  color: whitesmoke;
}
</style>

<div class="footer">
<a href="/">ご利用ガイド</a><a href="/">お問い合わせ</a><a href="/">会社概要</a><a href="/">ご利用規約</a>
<a href="/">プライバシーポリシー</a><a href="/">特定商取引法に基づく表記</a><a href="/">特定商取引法に基づく表記</a>
<div style="padding:10px;color:silver;">Copyright(c) Ltd. All rights reserved.</div>
</div>

<script>

function handleForm(event) { event.preventDefault(); } 
var form = document.getElementById("register");
form.addEventListener('submit', handleForm);

function registering () {
  var fd = new FormData(document.getElementById('register'));
  fetch('/test/',
    {
        method: 'POST',
        cache: 'no-cache',
        body: fd
    })
    .then((res) => {
        // if (!res.ok) {
        //     throw new Error(`${res.status} ${res.statusText}`);
        // }
        return res.json();
    })
    .then((res) => {
      console.log(res);
      console.log(res["Status"]);
      
    })
    // .catch((reason) => {
    //     console.log(reason);
    // })
    ;
}

function check(input) {
  if (input.value != document.getElementsByName("password1")[0].value) {
      input.setCustomValidity('Password Must be Matching.');
  } else {
      input.setCustomValidity('');
  }
}

// function showErr () {
//   document.querySelector('.error').style.display = "block";
// }

</script>

</body>
</html>

